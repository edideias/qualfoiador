{
  "version": 2,
  "builds": [
    {
      "src": "api/handler.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/**", "server/**", "*.json"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/handler.ts"
    }
  ]
}
